
* **Siapa yang akan mengisinya?** (Apakah Anggota Posyandu, Bidan, atau masyarakat umum?)
* **Data apa saja yang perlu kita kumpulkan** di dalam form tersebut? (Misalnya: Nama Pasien, NIK, Usia, Alamat, pertanyaan-pertanyaan skrining, hasil, dll.)
* **Bagaimana data ini akan dikelola** di admin panel nanti? Apakah hanya ditampilkan dalam tabel, atau akan ada analisis dan dashboard seperti yang kita diskusikan sebelumnya?

begini, yang mengisi ini adalah Bidan Ketua Posyandu dan Anggota Posyandu

dalam form yang akan kita bikin adalah
Tanggal Skrining : (menampilkan tanggal)
Diinput oleh    : ini menampikan nama dari role pengguna yang dipakai

IDENTITAS PASIEN

awlm irlf bbol qrpk

        Schema::disableForeignKeyConstraints();
        DB::table('roles')->truncate();
        DB::table('role_user')->truncate();
        Schema::enableForeignKeyConstraints();

Route::get('/dashboard', function () {
    return view('dashboard');
})->middleware(['auth', 'verified'])->name('dashboard');


5e70 0a1d 670a 8a08
